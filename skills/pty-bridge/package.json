{
  "name": "pty-bridge",
  "version": "1.1.0",
  "description": "PTY bridge — manage interactive terminal sessions via CLI",
  "main": "dist/cli.js",
  "bin": {
    "pty-bridge": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "pty",
    "terminal",
    "ssh",
    "repl",
    "interactive"
  ],
  "license": "MIT",
  "dependencies": {
    "@lydell/node-pty": "^1.0.0",
    "@xterm/headless": "^5.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0"
  }
}
